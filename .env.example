# Environment Variables for Burger Bangor POS

# ===========================================
# SUPABASE CONFIGURATION
# ===========================================
# Get these from your Supabase project settings > API
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Supabase Service Role Key (Server-side only - NEVER expose to client)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Supabase Storage bucket for product images
# Create this bucket in Supabase Storage and make it public so product/menu images can be displayed.
SUPABASE_PRODUCT_IMAGES_BUCKET=product-images

# ===========================================
# DATABASE CONFIGURATION (for Drizzle ORM)
# ===========================================
# Get this from Supabase project settings > Database > Connection string
# Use the "Transaction" connection string for server-side operations
DATABASE_URL=postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres

# ===========================================
# NEXTAUTH CONFIGURATION
# ===========================================
# Generate a secure secret: openssl rand -base64 32
# Or use: https://generate-secret.vercel.app/32
NEXTAUTH_SECRET=your-nextauth-secret-here

# The canonical URL of your site in production
NEXTAUTH_URL=https://your-domain.com

# ===========================================
# OPTIONAL: OAuth Providers (if needed later)
# ===========================================
# GOOGLE_CLIENT_ID=your-google-client-id
# GOOGLE_CLIENT_SECRET=your-google-client-secret
